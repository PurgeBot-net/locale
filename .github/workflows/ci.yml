name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  build:
    uses: PurgeBot-net/workflows/.github/workflows/build-library.yml@main
    secrets: inherit

  auto-tag:
    if: github.event_name == 'push'
    needs: build
    uses: PurgeBot-net/workflows/.github/workflows/auto-tag.yml@main
    secrets: inherit

  auto-label:
    if: github.event_name == 'pull_request'
    uses: PurgeBot-net/workflows/.github/workflows/auto-label.yaml@main
    secrets: inherit
